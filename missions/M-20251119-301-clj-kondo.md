# M-20251119-301 – Clj-kondo Guardrails & Repo Cleanup

## Summary
Add a governed linting stage backed by clj-kondo so every dictionary artifact, runtime namespace, and test file analyzes cleanly. Update the repo so there are zero clj-kondo errors, wire the new lint action into the standard mission test protocol, and document the enforcement path.

## Category
:mission.category/governance

## Scope
- `resources/dictionary/**/*.edn` – ensure all action/protocol/mission/permission entries lint under clj-kondo.
- `src/intuition/**/*.clj` – add missing namespace requires/aliases so clj-kondo passes.
- `test/**/*.clj` – fix analyzer failures and wire linting into the mission test harness.
- `SYSTEM_SPEC.md`, mission log – document lint governance and testing commands.
- `deps.edn`, tooling aliases – add/adjust the clj-kondo invocation.

## Prerequisites
- M-11 – Permission model consolidation (uses the same dictionary resources).

## Deliverables
- `deps.edn` – new `:lint` alias invoking clj-kondo with repo-wide config.
- `resources/dictionary/clj_kondo/config.edn` (or equivalent) – centralized lint configuration tied to SYSTEM_SPEC §§1.2, 5.3.
- `src/intuition/sfs/actions/runtime.clj`, `test/**/*.clj`, `resources/**/*.edn` – fixes for all clj-kondo findings (unused vars, unresolved keywords, malformed data).
- `dev/lint.clj` (optional helper) – one-command entry point for agents.
- `SYSTEM_SPEC.md` + `missions/logs/M-20251119-301.md` – documentation of the lint action, protocol step, and validation evidence.

## Work-Type Matrix
| Kind            | Required | Notes |
|-----------------|----------|-------|
| planning        | yes      | Scope + lock mission dictionary resources. |
| code            | yes      | Alias/tooling updates + lint fixes in src/test. |
| data            | yes      | Dictionary EDN cleanups + optional clj-kondo config. |
| test-functional | yes      | Run `clojure -M:lint` + `clojure -M:test`. |
| doc             | yes      | SYSTEM_SPEC + mission log updates. |

## Acceptance
- Run `clojure -M:lint` (clj-kondo) from repo root; output must show zero errors.
- Run `clojure -M:test` and attach tail of `/tmp/test.log` proving all suites still pass.
- Mission log captures before/after lint status plus the exact commands executed.
- SYSTEM_SPEC gains a section referencing the linting invariant and action so future missions inherit the guardrail.
